<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.elmanss.cargamos">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
      android:name="me.elmanss.cargamos.CargamosApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name="me.elmanss.cargamos.presentation.list.local.LocalListActivity" />
    <activity
        android:name="me.elmanss.cargamos.presentation.splash.SplashActivity"
        android:noHistory="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name="me.elmanss.cargamos.presentation.detail.DetailActivity" />
    <activity android:name="me.elmanss.cargamos.presentation.list.main.MainActivity" />
  </application>

</manifest>